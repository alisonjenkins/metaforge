pub mod backstage;
pub mod cli;
pub mod git;
pub mod languages;
pub mod project;
// use crate::project::Project;

// pub async fn scan_repo<'a>() -> Result<Vec<Project<'a>>, ScanRepoError> {
//     let mut projects: Vec<Project> = vec![];
//
//     // walk directory structure
//     // for each directory's files see if any of the language's dependency files exist
//     // if a dependency file exists add a project to the project_roots
// }
